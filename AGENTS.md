# 모잇(sodate) — 먼저 읽는 문서

> 이 저장소에서 일하기 전에 이 문서부터 읽는다.
> 오너는 개발자가 아니다. **항상 한국어·존댓말로, 결론을 먼저, 쉽게** 말한다.

---

## 이 서비스가 뭔가

**모잇** — 전국의 소개팅·소셜링·혼술바 일정을 크롤링해 한곳에 모아 보여주는 모바일 앱.
모임을 직접 운영하지 않는다. 정보를 모아 연결하고, 신청은 업체 사이트로 내보낸다(아웃링크).

- 내부 ID 는 영구히 `sodate` (앱 이름만 '소개팅모아' → '모잇' 으로 바뀌었다)
- 안드로이드 출시 완료(2026-09-01), iOS 진행

```
크롤러(Python) → Supabase → 앱(Expo)
                    ↑
      관리자 콘솔 / 제휴 포털 / 소개 사이트
```

---

## 반드시 먼저 읽을 문서 4개

| 문서 | 언제 |
|---|---|
| **`docs/OWNER_RULES.md`** | **작업 시작 전 무조건.** 오너와 일하는 방식·금지사항·지난 사고 |
| **`docs/CRAWLING.md`** | 크롤러를 건드리기 전 무조건. **이 서비스의 심장이다** |
| `docs/OPERATIONS.md` | 배포·자격증명 위치·인프라 |
| `docs/DATA_SCHEMA.md` | 표 구조 |

### 특히 이 세 가지는 사고로 배운 것이다

1. **확인 없이 파일을 고치거나 배포하지 않는다.** 오너의 질문·불만은 «고치라는 지시»가 아니다
2. **해보지 않고 «된다»고 말하지 않는다.** 안 해봤으면 「해봐야 안다」고 말한다
3. **크롤 자료(나이·가격·마감·날짜)는 100% 정확해야 한다.** 확신 없으면 비워두고 확인받는다

---

## 폴더

| 폴더 | 무엇 | 배포 |
|---|---|---|
| `app/` | Expo 앱 | EAS OTA (`production` 한 번이면 안드·iOS 동시) |
| `crawler/` | Python 크롤러·워치독 | GitHub Actions 스케줄 |
| `admin/` | 관리자 콘솔 (React + CF Pages Functions) | `sodate-admin` |
| `partner/` | 제휴 업체용 포털 | `sodate-partner` (partner.moitbiz.com) |
| `supabase/` | 마이그레이션·Edge Functions | |
| `docs/` | 사양·인수인계 문서 | |
| `agents/` | 예전 PM 에이전트 지시서 (참고용) | |

**저장소 밖**: 소개 사이트 `~/dev/moit-web` (별도 git, 원격 없음 — 푸시하면 실패하는 게 정상)

---

## 자주 틀리는 곳

- **업체 목록의 정본은 코드가 아니라 DB(`companies`) 다.** `scrapers/` 폴더엔 죽은 코드가 섞여 있다
- **`verified=true` 인 일정은 절대 건드리지 않는다** (오너가 손으로 고친 것 + 제휴사가 직접 올린 것)
- **Cloudflare Pages 배포엔 `CLOUDFLARE_ACCOUNT_ID` 를 같이 줘야 한다** — 없으면 인증 오류로 죽는다
- **moitbiz.com/partner 문구는 HTML 을 고치지 않는다.** admin «제휴 소개 페이지» 에서 고친다
- **Supabase 프로젝트가 2026-09-04 에 바뀌었다** — ref `kmakdtcavtheaqobktlj`. 옛 ref 쓰지 말 것
- **미디어는 전부 Cloudflare R2.** Supabase Storage 안 쓴다
- 자격증명은 **이미 다 저장돼 있다**(`~/.config/sodate/README.md`). 오너에게 다시 묻지 말 것

---

## 지금 상태 (2026-09-07)

- 앱: 안드 출시 완료 · iOS 진행 · OTA 로 수시 반영 중
- 제휴 포털: partner.moitbiz.com 라이브
- 소개 사이트: moitbiz.com 라이브, `/partner` 문구는 admin 에서 수정 가능
- 크롤러: 17개 업체. 에모셔널오렌지는 2026-09 사이트 개편에 맞춰 새 방식으로 옮김
- admin 분석 화면에 방문자수·신규·체류시간(일별/주별/월별·기간 지정) 있음

> `docs/HANDOFF.md` 는 2026-08-07 앱스토어 심사 시점의 기록이다. 지금 상태와 다르다.
